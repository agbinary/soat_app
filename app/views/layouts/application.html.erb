<!DOCTYPE html>
<html>
<head>
  <title>Soatapp</title>
  <meta charset='utf-8'>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="header clearfix">
      <nav>
        <ul class="nav nav-pills pull-right">
          <li role="presentation" class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown">Administrador<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
              <% if admin_signed_in? %>
                <li><a tabindex="-1" href="#"><%= link_to 'Ver Soats', :admin_soats %></a></li>
                <li><a tabindex="-1" href="#"><%= link_to 'Cerrar sesion Administrador', :destroy_admin_session, method: :delete %></a></li>
              <% else %>
                <li><a tabindex="-1" href="#"><%= link_to 'Iniciar sesion Administrador', :admin_soats %></a></li>
              <% end %>
            </ul>
          </li>
          <li role="presentation"><%= link_to 'Comprar Soat', :new_soat %></li>
        </ul>
      </nav>
      <h3 class="text-muted"><%= link_to 'SOAT', :root %></h3>
    </div>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <%= yield %>

    <br><br>
    <footer>
      <p style="text-align: center">&copy; Angela <%= Time.now.year %></p>
    </footer>
  </div>
</body>
</html>
